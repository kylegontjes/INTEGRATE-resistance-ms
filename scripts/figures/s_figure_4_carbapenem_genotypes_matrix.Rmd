---
title: "Supplemental Figure 4: Carbapenem resistance-associated genotype matrix"
author: "Kyle Gontjes"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
require(knitr)
opts_knit$set(echo = T,warning = FALSE, message = FALSE,error=F,comment=NA) 
opts_chunk$set(echo = T,warning = FALSE, message = FALSE,error=F,comment=NA)
opts_knit$set(root.dir='~/Desktop/gl_nfs/Project_INTEGRATE/Analysis/Resistance_dynamics/INTEGRATE-resistance-ms')
```

# Environment

```{r}
# Packages
packages <- c("tidyverse","gridExtra","kableExtra","tableone","cowplot","ComplexHeatmap",'ggalluvial')
lapply(packages,library,character.only=T)

# Functions
source("./lib/consistent_themes.R")
source("./lib/common_functions.R")

# Print environment
Sys.info()
sessionInfo()
```

# Load data

```{r}
df <- readRDS("./data/dataset/CA_LTACH_first_clinical_per_quarter_CRKP_genomes_merged_studies_metadata.RDS")  

df_201415 <- df%>% subset(study == "2014-15")
df_202123 <- df%>% subset(study == "2021-23") 
```

# Load  carbapenemase content
```{r}
carbapenemase_content <- readRDS("./data/carbapenemase_content/carbapenemase_content_merged_studies.RDS")
df <- left_join(df,carbapenemase_content)
```

# Load omp matrix

```{r}
carb_omp <-  readRDS("./data/carbapenemase_content/carbapenem_resistance_genotype_content_merged_first_clinical_CRKP_isolate_per_quarter.RDS")
``` 

# Supplemental Figure:

```{r,fig.width=9.5,height=6.7857}  
df_carb_omp <- df %>% select(isolate_no,carbapenemase,study,Species_ST_recode) %>% left_join(., carb_omp %>% mutate(isolate_no = rownames(.)))    %>% as.data.frame
rownames(df_carb_omp) <- df_carb_omp$isolate_no 
```

# Get matrix for figure
  
```{r,fig.width=9,height=7.2} 
# Scales 
## Genotypes
genotypes <- carbapenemase_scale$breaks  %>% gsub("<i>bla</i><sub>","bla",.) %>% gsub("</sub>","",.) %>% trimws(.,"both")
genotype_colors <- setNames(carbapenemase_scale$palette(1), genotypes)
 
## Study coloring
study_colors <-  study_comparison_scale_fill$palette(1)

# Row annotation
legend_common_features <- list(
  title_gp = gpar(fontsize = 9),
  labels_gp = gpar(fontsize = 9),
  grid_width = unit(0.25, "cm"),
  grid_height = unit(0.25, "cm")
)

row_annots <- rowAnnotation(Study = df_carb_omp$study,
                            `Sequence type (ST)` = df_carb_omp$Species_ST_recode,
                            Carbapenemase = df_carb_omp$carbapenemase,
                            col = list(Study = study_colors,
                                       Carbapenemase = genotype_colors,
                                       `Sequence type (ST)`=ST_palette %>% subset(names(.) %in% c(unique(df_carb_omp$Species_ST_recode)))), 
                            annotation_name_gp = gpar(fontsize = 9),
                            annotation_legend_param =list(
    Study = c(legend_common_features,list(ncol=2)), 
    `Sequence type (ST)` =c(legend_common_features,list(at = ST_scale$breaks,ncol=2)),  
    Carbapenemase =c(legend_common_features,list(at=carbapenemase_scale$breaks %>% gsub("<i>bla</i><sub>","bla",.) %>% gsub("</sub>","",.) %>% trimws(.,"both") ,ncol=1)))
 )

# Create genotype matrix
genotype_matrix <- df_carb_omp %>%  select(-carbapenemase,-study,-Species_ST_recode) %>% mutate_all(as.numeric) %>% select_if(colSums(.,na.rm = T)>0) %>% subset(is.na(rowSums(.))==F)
colnames(genotype_matrix) <- recode_genotypes(colnames(genotype_matrix))

# Heatmap
porin_bla_genotype <- ComplexHeatmap::Heatmap(genotype_matrix,
                                              col=c("white","black"),
                                              show_row_names = F,
                                              show_row_dend = T,
                                              show_column_dend = T,
                                              show_parent_dend_line = F,
                                              name = "Genotype",
                                              gap = unit(0.2, "mm") ,
                                              right_annotation = row_annots,
  heatmap_legend_param = list(
    labels = c("1" = "Present","0" = "Absent"),
    nrow = 1,
     gap = unit(3, "mm"),
    labels_gp = gpar(fontsize = 9),
    title_gp = gpar(fontsize =9),
    grid_width = unit(0.25,"cm"),
    grid_height = unit(0.25,"cm"),
    border = "black"
    
),row_names_gp = gpar(fontsize = 0),
  column_names_gp = gpar(fontsize = 9),
  row_split = df$Species_ST_recode,
  row_title = NULL,
  cluster_row_slices = T,
  cluster_column_slices = T)

porin_bla_genotype
```

# Save
  
```{r,fig.width=9,height=7.2}
png("./figures/s_figure_4_carbapenem_resistance_associated_genotypes_matrix.png", width = 9, height = 7.2, units = 'in',res = 900)
draw(porin_bla_genotype)
dev.off() 
```
